扫榜结束后，批量下载榜单每一本书前60章内容到book目录，然后分析是否是ai写的文章，ai写的文章独立记录一个文件列表和说明，不是ai写的文章独立记录一个文件列表
然后开始开始分析语句表达，对比ai和人工的语句，在一句话相同意思的情况下，抽离出来大量的语句和表达json文件，用于后期ai语句替换，出一下更加完善，更加详细的，更多的对照，比如增加转折，人物出场描述等等，扩充的更多一些

---

## 番茄下载器用法（已验证 v2.4.15）

工具：`F:\book\TomatoNovelDownloader-Win64-v2.4.15.exe`  
输出格式：`config.yml` 里已是 `novel_format: txt`（可直接下 txt）。

### 结论

| 方式 | 能否新建下载 | 说明 |
|------|--------------|------|
| CLI `--download <book_id>` | ❌ | 防滥用已禁用 |
| CLI `--update <book_id>` | 仅已下过的书 | 本地无记录会拒绝 |
| TUI（双击） | ✅ | 交互下载 |
| Web UI `--server` | ✅ **推荐批量** | 支持按章节区间 |

**批量自动化请用 Web UI API，不要用纯 CLI。**

### 推荐流程

```powershell
# 1. 启动（在 F:\book 目录）
.\TomatoNovelDownloader-Win64-v2.4.15.exe --server
# 浏览器：http://127.0.0.1:18423/

# 2. 保存目录改为项目 book（可选，POST /api/config/full 改 save_path）
# 3. 按 bookId 创建任务（前60章）：
# POST http://127.0.0.1:18423/api/jobs
# {"book_id":"7672596897554517054","range_start":1,"range_end":60}
```

注意：

- 同时只能有约 1 个排队/运行任务，多推会 **429**；需串行：一本 `done` 再创建下一本。
- 书不足 60 章时会下完全部（例：47/47）。
- 下载记录/封面会写在 `book/<bookId>/`；正文 txt 在 `book/<书名>.txt`。

### 本次进度（2026-09-16）

- 榜单1：番茄女/男 · 新书榜+阅读榜 · 游戏体育（4 份）→ 40 本
- 榜单2：番茄女/男 · 阅读榜 · **全题材**（2 份，共 370 去重）→ 已下增量，合计本地 txt **345** 本
- 未下完：**45** 本，见  
  - `book/_pending_download_全题材_20260916.md`  
  - `book/_pending_download_全题材_20260916.json`  
  下次续跑 `book/_tools/batch_download_fullrank.py`

### AI 甄别与语句库（已按当前 345 本重建）

产出目录：`book/_analysis/`

| 文件 | 内容 |
|------|------|
| `ai_books.md` / `ai_books.txt` | 疑似 AI/高光滑模板 |
| `mixed_books.md` / `mixed_books.txt` | 混合/高套路 |
| `human_books.md` / `human_books.txt` | 更偏人工 |
| `verdicts_final.json` | 全量判定+说明 |
| `ai_human_phrase_bank.json` | 全量桶+精选组+pairs（**v2.1**：4395 pairs / 28 桶 / AI源50+人源160） |
| `ai_to_human_replacements.json` | 精简替换词典（**2734** pairs / **483** 键，优先用） |
| `phrase_bank_index.md` | 分类索引 + 精选对照表 |

说明：分流用于语料对照，**不是**作者身份实锤。工具脚本在 `book/_tools/`。
